export const getTextAreaMess = (state) => {
    return state.dialog.textAreaMess
}

export const getPrivateMessageData = (state) => {
    return state.dialog.privateMessageData
}

export const getDialogData = (state) => {
    return state.dialog.DialogData
}