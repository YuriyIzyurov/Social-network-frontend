export const getCurrentProfile = (state) => {
    return state.profile.currentProfile
}

export const getTextArea = (state) => {
    return state.profile.textArea
}

export const getMessagesData = (state) => {
    return state.profile.messagesData
}